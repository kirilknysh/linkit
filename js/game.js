define(["lodash", "backbone"],
    function(_, Backbone) {
        var game = {};

        _.extend(game, {
            initialize: function (router) {
                
            }
        });

        return game;
});